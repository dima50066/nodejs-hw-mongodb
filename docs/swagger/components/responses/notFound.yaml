description: Not found response for contacts.
content:
  application/json:
    schema:
      type: object
      properties:
        message:
          type: string
          example: 'Contact not found.'
